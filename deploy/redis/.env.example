# Redis password - use a strong random password
REDIS_PASSWORD=your-strong-redis-password-here

# API token for serverless-redis-http (used as KV_REST_API_TOKEN in Vercel)
SRH_TOKEN=your-api-token-here
